# Start minikube
minikube start

# Set docker env
eval $(minikube docker-env)

# Build image
docker build -t (image-name) .

# Run in minikube
kubectl run (new_name) --image=(image-name) --image-pull-policy=Never

# Check that it's running
kubectl get pods